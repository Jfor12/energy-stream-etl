// Replaced at deploy time by .github/workflows/pages.yml with the repository
// variables SUPABASE_URL and SUPABASE_ANON_KEY (the public, read-only key).
window.DASHBOARD_CONFIG = { supabaseUrl: '', supabaseKey: '' };
